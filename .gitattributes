# Normalize line endings across the team (Windows + WSL + CI).
# Fixes the phantom "23 files modified" CRLF/LF churn in git status.
* text=auto

# Explicitly text
*.py text
*.md text
*.json text
*.txt text

# Binary — never touch
*.png binary
*.docx binary
*.pdf binary
