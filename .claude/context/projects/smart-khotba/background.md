# Smart Khotba — Project Background

> Context for all QA analysis. Read this before analyzing any feature. Items marked
> *(confirm)* need verification with the team.

## Company / Domain

<!-- TODO: fill via the discovery process (see scratch/asiacell_discovery.py as the model) -->

## Platforms (Surfaces)

<!-- TODO: fill via the discovery process (see scratch/asiacell_discovery.py as the model) -->

## Services / Products

<!-- TODO: fill via the discovery process (see scratch/asiacell_discovery.py as the model) -->

## Service ↔ Platform Matrix

<!-- TODO: fill via the discovery process (see scratch/asiacell_discovery.py as the model) -->

## Core Business Objects

<!-- TODO: fill via the discovery process (see scratch/asiacell_discovery.py as the model) -->

## User Roles

<!-- TODO: fill via the discovery process (see scratch/asiacell_discovery.py as the model) -->

## Integrations

<!-- TODO: fill via the discovery process (see scratch/asiacell_discovery.py as the model) -->

## Environments *(confirm)*

<!-- TODO: fill via the discovery process (see scratch/asiacell_discovery.py as the model) -->

## Compatibility & Localization

<!-- TODO: fill via the discovery process (see scratch/asiacell_discovery.py as the model) -->

## Notes / To Confirm

<!-- TODO: fill via the discovery process (see scratch/asiacell_discovery.py as the model) -->
